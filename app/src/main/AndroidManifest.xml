<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gad_af_project">



    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GAD_AF_project">

        <activity android:name=".SplashActivity" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VEHICLE_LIST" />
            </intent-filter>
        </activity>
        <activity android:name=".VehicleActivity">
            <intent-filter>
                <action android:name="android.intent.action.VEHICLE" />
            </intent-filter>
        </activity>

        <activity android:name=".NewVehicleActivity">
            <intent-filter>
                <action android:name="android.intent.action.NEW_VEHICLE" />
            </intent-filter>
        </activity>
    </application>

</manifest>